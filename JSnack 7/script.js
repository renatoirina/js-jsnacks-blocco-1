// Definisco la variabile "n"
let n = 2;

// Eseguo un ciclo for con la costante i=1000 e la variabile n stampa ad ogni ciclo il suo valore e poi si moltiplica per se stesso
for(let i = 1000; n <= i; n*=2) {
    console.log(n);
}